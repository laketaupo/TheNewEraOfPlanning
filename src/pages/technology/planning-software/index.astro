---
import BaseLayout from '../../../layouts/BaseLayout.astro';
import ThemeToggle from '../../../components/ThemeToggle.astro';
import { getChapters, getTopics } from '../../../lib/chapters';

const chapters = getChapters('technology').filter(ch => ch.module === 'planning-software');
const allTopics = getTopics();

const iconMap: Record<string, string> = {
  cube: `<svg xmlns="http://www.w3.org/2000/svg" class="w-7 h-7" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M20 7l-8-4-8 4m16 0v10l-8 4M4 7v10l8 4"/></svg>`,
  network: `<svg xmlns="http://www.w3.org/2000/svg" class="w-7 h-7" fill="none" viewBox="0 0 24 24" stroke="currentColor"><circle cx="12" cy="5" r="2" stroke-width="1.5"/><circle cx="5" cy="19" r="2" stroke-width="1.5"/><circle cx="19" cy="19" r="2" stroke-width="1.5"/><path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M12 7v4m0 4l-5 2m10-2l-5 2"/></svg>`,
  cpu: `<svg xmlns="http://www.w3.org/2000/svg" class="w-7 h-7" fill="none" viewBox="0 0 24 24" stroke="currentColor"><rect x="6" y="6" width="12" height="12" rx="1" stroke-width="1.5"/><path stroke-linecap="round" stroke-width="1.5" d="M9 6V4M12 6V4M15 6V4M9 20v-2M12 20v-2M15 20v-2M6 9H4M6 12H4M6 15H4M20 9h-2M20 12h-2M20 15h-2"/></svg>`,
  beaker: `<svg xmlns="http://www.w3.org/2000/svg" class="w-7 h-7" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M9 3h6m-6 0v7l-4 9a1 1 0 001 1h12a1 1 0 001-1l-4-9V3"/></svg>`,
};

const colorBgMap: Record<string, string> = {
  indigo: 'bg-indigo-50 dark:bg-indigo-500/10 border-indigo-200 dark:border-indigo-500/30 hover:border-indigo-400 dark:hover:border-indigo-400',
  violet: 'bg-violet-50 dark:bg-violet-500/10 border-violet-200 dark:border-violet-500/30 hover:border-violet-400 dark:hover:border-violet-400',
  sky:    'bg-sky-50 dark:bg-sky-500/10 border-sky-200 dark:border-sky-500/30 hover:border-sky-400 dark:hover:border-sky-400',
  emerald:'bg-emerald-50 dark:bg-emerald-500/10 border-emerald-200 dark:border-emerald-500/30 hover:border-emerald-400 dark:hover:border-emerald-400',
  amber:  'bg-amber-50 dark:bg-amber-500/10 border-amber-200 dark:border-amber-500/30 hover:border-amber-400 dark:hover:border-amber-400',
  teal:   'bg-teal-50 dark:bg-teal-500/10 border-teal-200 dark:border-teal-500/30 hover:border-teal-400 dark:hover:border-teal-400',
};

const colorTextMap: Record<string, string> = {
  indigo: 'text-indigo-600 dark:text-indigo-400',
  violet: 'text-violet-600 dark:text-violet-400',
  sky:    'text-sky-600 dark:text-sky-400',
  emerald:'text-emerald-600 dark:text-emerald-400',
  amber:  'text-amber-600 dark:text-amber-400',
  teal:   'text-teal-600 dark:text-teal-400',
};
---
<BaseLayout title="Planning Software — Technology">
  <header class="fixed top-0 left-0 right-0 z-50 flex items-center justify-between px-4 py-3 bg-white/90 dark:bg-gray-950/90 backdrop-blur border-b border-gray-200 dark:border-gray-800">
    <div class="flex items-center gap-3">
      <a href={import.meta.env.BASE_URL} title="Home" class="flex items-center text-gray-500 dark:text-gray-400 hover:text-gray-900 dark:hover:text-white transition-colors">
        <svg xmlns="http://www.w3.org/2000/svg" class="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h6"/>
        </svg>
      </a>
      <a href="/technology" class="flex items-center gap-1.5 text-sm text-gray-500 dark:text-gray-400 hover:text-gray-900 dark:hover:text-white transition-colors">
        <svg xmlns="http://www.w3.org/2000/svg" class="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7"/>
        </svg>
        Technology
      </a>
    </div>
    <ThemeToggle />
  </header>

  <div class="relative overflow-hidden pt-12">
    <div class="absolute inset-0 bg-[linear-gradient(to_right,#e5e7eb_1px,transparent_1px),linear-gradient(to_bottom,#e5e7eb_1px,transparent_1px)] dark:bg-[linear-gradient(to_right,#1f2937_1px,transparent_1px),linear-gradient(to_bottom,#1f2937_1px,transparent_1px)] bg-[size:64px_64px] opacity-60 dark:opacity-40 pointer-events-none"></div>
    <div class="absolute inset-0 bg-radial-gradient pointer-events-none"></div>

    <div class="relative z-10 px-6 pt-12 pb-12 max-w-3xl mx-auto text-center animate-fade-in">
      <div class="inline-flex items-center gap-2 text-xs font-medium text-sky-600 dark:text-sky-400 bg-sky-50 dark:bg-sky-500/10 border border-sky-200 dark:border-sky-500/30 rounded-full px-3 py-1 mb-6">
        <span class="w-1.5 h-1.5 rounded-full bg-sky-500 animate-pulse"></span>
        Technology
      </div>

      <h1 class="text-5xl sm:text-6xl font-bold text-gray-900 dark:text-white mb-6 leading-tight tracking-tight">
        <span class="text-transparent bg-clip-text bg-gradient-to-r from-sky-500 to-indigo-500">Planning Software</span>
      </h1>
      <p class="text-xl text-gray-600 dark:text-gray-400 max-w-xl mx-auto leading-relaxed">
        From the fundamental building blocks of a supply chain network to live scenario simulation — a guided tour of the platform, plus the configuration manual.
      </p>
    </div>

    <div class="relative z-10 px-6 pb-20 max-w-6xl mx-auto">
      <div class="flex flex-col gap-4 max-w-3xl mx-auto">

        <!-- Configuration Manual card -->
        <a
          href={`${import.meta.env.BASE_URL}technology/configuration`}
          class="group relative flex flex-col p-6 rounded-2xl border bg-indigo-50 dark:bg-indigo-500/10 border-indigo-200 dark:border-indigo-500/30 hover:border-indigo-400 dark:hover:border-indigo-400 bg-white dark:bg-transparent transition-all duration-200 hover:-translate-y-1 hover:shadow-xl dark:hover:shadow-2xl"
        >
          <span class="text-xs font-mono font-bold text-indigo-600 dark:text-indigo-400 mb-4">CONFIG</span>
          <div class="flex items-start gap-4 mb-4">
            <div class="text-indigo-600 dark:text-indigo-400 mt-0.5 shrink-0">
              <svg xmlns="http://www.w3.org/2000/svg" class="w-7 h-7" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"/>
              </svg>
            </div>
            <div>
              <h3 class="text-lg font-semibold text-gray-900 dark:text-white leading-snug">Configuration Manual</h3>
              <p class="text-sm text-gray-500 dark:text-gray-400 mt-1 leading-relaxed">Step-by-step screenshots and explanations for configuring the planning software for your business.</p>
            </div>
          </div>
          <div class="flex items-center justify-between mt-auto pt-4 border-t border-gray-100 dark:border-white/5">
            <span class="text-xs text-gray-400 dark:text-gray-500">Business configuration guide</span>
            <div class="flex items-center gap-1 text-xs text-indigo-600 dark:text-indigo-400 opacity-0 group-hover:opacity-100 transition-opacity">
              Open guide
              <svg xmlns="http://www.w3.org/2000/svg" class="w-3.5 h-3.5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"/>
              </svg>
            </div>
          </div>
        </a>

        <!-- Chapter cards -->
        {chapters.map((chapter, i) => {
          const topics = allTopics.filter(t => t.chapterSlug === chapter.slug);
          const bgClass = colorBgMap[chapter.color] ?? 'bg-brand-50 dark:bg-brand-500/10 border-brand-200 dark:border-brand-500/30';
          const textClass = colorTextMap[chapter.color] ?? 'text-brand-600 dark:text-brand-400';
          return (
            <a
              href={`${import.meta.env.BASE_URL}technology/planning-software/${chapter.slug}`}
              data-hidden-chapter={chapter.hidden ? 'true' : undefined}
              class={`group relative flex flex-col p-6 rounded-2xl border ${bgClass} bg-white dark:bg-transparent transition-all duration-200 hover:-translate-y-1 hover:shadow-xl dark:hover:shadow-2xl${chapter.hidden ? ' hidden-chapter hidden' : ''}`}
            >
              <span class={`text-xs font-mono font-bold ${textClass} mb-4`}>0{chapter.order}</span>
              <div class="flex items-start gap-4 mb-4">
                <div class={`${textClass} mt-0.5 shrink-0`} set:html={iconMap[chapter.icon] ?? iconMap['cube']} />
                <div>
                  <h3 class="text-lg font-semibold text-gray-900 dark:text-white leading-snug">{chapter.title}</h3>
                  <p class="text-sm text-gray-500 dark:text-gray-400 mt-1 leading-relaxed">{chapter.description}</p>
                </div>
              </div>
              <div class="flex items-center justify-between mt-auto pt-4 border-t border-gray-100 dark:border-white/5">
                <span class="text-xs text-gray-400 dark:text-gray-500">{topics.length} topics</span>
                <div class={`flex items-center gap-1 text-xs ${textClass} opacity-0 group-hover:opacity-100 transition-opacity`}>
                  Open chapter
                  <svg xmlns="http://www.w3.org/2000/svg" class="w-3.5 h-3.5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"/>
                  </svg>
                </div>
                <canvas
                  class="chapter-progress-ring hidden"
                  data-chapter-slug={chapter.slug}
                  data-topic-count={topics.length}
                  data-chapter-index={i}
                  width="32"
                  height="32"
                ></canvas>
              </div>
            </a>
          );
        })}
      </div>
    </div>
  </div>

  <footer class="border-t border-gray-200 dark:border-gray-800 px-6 py-8 text-center text-sm text-gray-400 dark:text-gray-600">
    <p>Built with Astro · Content in Markdown · Maintained with Claude</p>
  </footer>
</BaseLayout>

<style>
  .bg-radial-gradient {
    background: radial-gradient(ellipse 80% 50% at 50% -20%, rgba(14,165,233,0.08), transparent);
  }
  :global(.dark) .bg-radial-gradient {
    background: radial-gradient(ellipse 80% 50% at 50% -20%, rgba(14,165,233,0.15), transparent);
  }
</style>

<script>
  const STORAGE_KEY = 'platform-progress';

  function getProgress(): Record<string, boolean> {
    try { return JSON.parse(localStorage.getItem(STORAGE_KEY) || '{}'); }
    catch { return {}; }
  }

  function drawRing(canvas: HTMLCanvasElement, percent: number, color: string) {
    const ctx = canvas.getContext('2d');
    if (!ctx) return;
    const size = canvas.width;
    const cx = size / 2, cy = size / 2, r = size / 2 - 3;
    ctx.clearRect(0, 0, size, size);
    ctx.beginPath();
    ctx.arc(cx, cy, r, 0, Math.PI * 2);
    ctx.strokeStyle = document.documentElement.classList.contains('dark')
      ? 'rgba(255,255,255,0.06)' : 'rgba(0,0,0,0.08)';
    ctx.lineWidth = 3;
    ctx.stroke();
    if (percent > 0) {
      ctx.beginPath();
      ctx.arc(cx, cy, r, -Math.PI / 2, -Math.PI / 2 + Math.PI * 2 * percent);
      ctx.strokeStyle = color;
      ctx.lineWidth = 3;
      ctx.lineCap = 'round';
      ctx.stroke();
    }
  }

  const chapterColors: Record<string, string> = {
    '0': '#6366f1',
    '1': '#8b5cf6',
    '2': '#0ea5e9',
    '3': '#10b981',
  };

  let hiddenVisible = false;
  document.addEventListener('keydown', (e) => {
    if (e.key !== 'y' && e.key !== 'Y') return;
    if ((e.target as HTMLElement).tagName === 'INPUT' || (e.target as HTMLElement).tagName === 'TEXTAREA') return;
    hiddenVisible = !hiddenVisible;
    document.querySelectorAll<HTMLElement>('.hidden-chapter').forEach((el) => {
      el.classList.toggle('hidden', !hiddenVisible);
    });
  });

  const progress = getProgress();

  document.querySelectorAll<HTMLCanvasElement>('.chapter-progress-ring').forEach((canvas) => {
    const slug = canvas.dataset.chapterSlug ?? '';
    const count = parseInt(canvas.dataset.topicCount ?? '0', 10);
    const idx = canvas.dataset.chapterIndex ?? '0';
    if (!count) return;

    const chapterNum = slug.split('-')[0];
    const completed = Object.keys(progress).filter(
      (k) => k.startsWith(`ch${parseInt(chapterNum, 10)}-`) && progress[k]
    ).length;

    const pct = count > 0 ? completed / count : 0;
    canvas.classList.remove('hidden');
    drawRing(canvas, pct, chapterColors[idx] ?? '#6366f1');
  });
</script>
