---
title: "KPIs and Exception Management"
description: "KPIs and exception-based working are two sides of the same management system. KPIs measure outcomes; exceptions drive the daily actions that determine those outcomes."
chapter: "process-06-kpis"
estimatedMinutes: 3
topicLayout: "prose-topic"
---

## The relationship between KPIs and exceptions

KPIs tell you how you performed. Exceptions tell you what to do today to improve tomorrow's performance.

OTIF is a lagging KPI — it measures what has already been delivered or missed. A shortage exception in Planning software is a leading signal — it tells the planner that a future OTIF miss is at risk if nothing is done. The connection between the two is direct: resolving exceptions is how you protect KPI performance.

This means exception management is not just an operational discipline — it is the mechanism through which KPI targets are actually achieved. An organisation that sets a 95% OTIF target but has no functioning exception management process is making a commitment it cannot keep.

## From KPI to exception threshold

The target KPI determines what the exception threshold should be. If the OTIF target is 95%, then any item projected to fall below that threshold is an exception. The exception threshold is derived from the target — it is not an independent configuration.

In Planning software, exception thresholds are parameters that can be set at the item, category, or global level. Setting them correctly — at a level that reflects the KPI target rather than operational habit — is a planning governance decision.

## Closing the loop

The KPI review in S&OP should explicitly connect to exception management performance:

1. **What was the OTIF this month?**
2. **How many shortage exceptions were flagged during the month?**
3. **Of those, how many were resolved before becoming service failures?**
4. **Of the OTIF misses, what percentage had a preceding exception that was not resolved in time?**

This last question is especially revealing. If most OTIF misses had a preceding exception, the problem is in exception resolution — the right information was available, but the process failed to act on it. If most misses had no preceding exception, the problem is in exception detection — the planning model is not surfacing risks early enough.

Both diagnoses point to specific actions. The KPI review only has operational value when it drives this kind of root cause analysis, not just a retrospective report of numbers.
